<nav class="navbar navbar-default navbar-fixed-top" role="navigation" style="background-color: #464646;">
  <div class="container">
    <div class="navbar-header page-scroll">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%=link_to "#{image_tag('title-logo-3.png', :alt=>'Student Opinion*')}".html_safe, root_path%>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav">
        <!-- Hidden li included to remove active class from about link when scrolled up past about section -->

        <li class="hidden">
          <a class="page-scroll" href="#page-top"></a>
        </li>

        <%if user_signed_in?%>
            <li><%= link_to ' Home', root_path, :class => 'navbar-link header-menu'%></li>
            <li><%= link_to 'Schools', schools_path, :class => 'navbar-link header-menu'%></li>
            <li><%= link_to 'Reviews', reviews_path, :class => 'navbar-link header-menu'%></li>

            <%if can? :index, @Engine%>
                <li><%= link_to 'Users', engine_index_path, :class => 'navbar-link header-menu'%></li>
            <%end%>
            <%if can? :index, Post%>
                <li><%=link_to 'Forum', posts_path, :class=>'navbar-link header-menu'%></li>
            <%end%>

            <li class="dropdown">
              <a class="dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                <%= current_user.public_name %><span class="caret"></span>
              </a>

              <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
                <%if Rails.env!='production'%>
                    <li><%= link_to 'Mailbox', message_boxes_path, :class => 'navbar-link' %></li>
                <%end%>
                <li><%= link_to 'Profile', edit_user_registration_path, :class => 'navbar-link' %></li>
                <li><%= link_to 'Logout', destroy_user_session_path, method: :delete, :class => 'navbar-link'%></li>
                <li style="font-size: 11px;"><i>[<%= current_user.email%>]</i></li>
              </ul>
            </li>
        <%else%>
              <%=link_to '<button type="button" class="btn btn-primary btn-xs">Login</button>'.html_safe, root_path+'users/sign_in'%>
              <%=link_to '<button type="button" class="btn btn-success btn-xs">Sign up</button>'.html_safe, root_path+'users/sign_up'%>
        <%end%>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container -->
</nav>