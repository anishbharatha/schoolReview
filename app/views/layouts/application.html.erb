<!DOCTYPE html>
<html>
<head>
  <title>SchoolReview</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application','data-turbolinks-track'=> true %>
  <link href='http://fonts.googleapis.com/css?family=Sigmar+One' rel='stylesheet' type='text/css'>
  <%= csrf_meta_tags %>
</head>

<body>
  <div class='row header-row'>
    <div class='col-md-6' style='margin-top: 10px;'>
      <%= image_tag('title-logo-1.png', :alt=>'Django!*')%>
    </div>

    <% if user_signed_in? %>
    <div class='col-md-6'>
      <div class='row col-md-12 text-right'>
        <p class='text-danger'>Logged in as <strong><%= current_user.email %></strong>.
          <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
          <%= link_to 'Logout', destroy_user_session_path, method: :delete, :class => 'navbar-link'%>
        </p>
      </div>
        <div class='row col-md-12 text-right header-pipes'>
          <%= link_to 'Home', root_path, :class => 'navbar-link header-menu'%>&nbsp;&nbsp;
          <%= link_to 'Schools', schools_path, :class => 'navbar-link header-menu'%>&nbsp;&nbsp;
          <%= link_to 'Reviews', reviews_path, :class => 'navbar-link header-menu'%>&nbsp;&nbsp;
          <%= link_to 'Users', engine_index_path, :class => 'navbar-link header-menu'%>
        </div>
    </div>
    <%else%>
    <div class='col-md-6'>
        <%=yield%>
    </div>
    <%end%>
  </div>

  <div class='container'>
    <div class='row col-md-12' style="margin-top: 20px;">
        <% if notice %> <p class="alert alert-success"><%= notice %></p> <% end %>
        <% if alert %>  <p class="alert alert-danger"><%= alert %></p>   <% end %>
    </div>
    <% if user_signed_in?%>
      <div class='row col-md-12'>
        <%= yield %>
      </div>
    <%else%>
        <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
            <%=link_to 'Sign up', new_registration_path(resource_name), class: 'text-primary pull-right',
               id:'sign-up'%>
            <!-- Button trigger modal -->
            <a type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#signupModal">
              Sign Up
            </a>
        <%end-%>



        <!-- Modal -->


                <%= render 'devise/registrations/new' %>

    <%end%>
    </div>
<style>
  .header-row{
      width: 100%;
      background-color: #464646;
      #background-color: #333333;
      opacity: 1;
      padding-bottom: 10px;
      padding-right: 10px;
  }
  .header-pipes{
      font-family: 'Sigmar One', cursive;
      font-size: 15px;
      color: #ffffff;
  }
  .header-menu{
      font-family: 'Sigmar One', cursive;
      font-size: 25px;
      color: #ffffff;
  }
  .header-menu:hover{
      color: #ffffff;
      text-decoration: none;
      border-bottom: 4px solid #ffcc00;
  }
</style>
</body>
</html>